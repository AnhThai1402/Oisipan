@model FrontendMvc.Models.OrderAdminViewModel
@{
    Layout = null;
    ViewData["Title"] = $"Hóa don #OP-{Model.OrderId.ToString("N")}";
}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
<link rel="stylesheet" href="~/css/invoice.css" asp-append-version="true" />

<div class="invoice-root" style="font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;">
    <header class="invoice-header">
        <div class="brand" style="display:flex;gap:12px;align-items:center;">
            <img src="~/images/logo.png" alt="logo" style="height:56px; width:auto; object-fit:contain;" onerror="this.style.display='none'" />
            <div>
                <h2>Oisipan</h2>
                <p>Ð?a ch?: S? 1, Ph? A, Thành ph?</p>
                <p>Hotline: 0123-456-789</p>
            </div>
        </div>
        <div class="meta">
            <h3>HÓA ÐON BÁN HÀNG</h3>
            <div>Mã don: <strong>#OP-@Model.OrderId.ToString("N")</strong></div>
            <div>Ngày: <strong>@Model.OrderDate.ToString("dd/MM/yyyy HH:mm")</strong></div>
        </div>
    </header>

    <section class="invoice-info">
        <div class="box">
            <h4>Khách hàng</h4>
            <p>@(string.IsNullOrWhiteSpace(Model.CustomerName) ? "Khách vãng lai" : Model.CustomerName)</p>
            <p>@Model.CustomerPhone</p>
            <p>@Model.ShippingAddress</p>
        </div>
        <div class="box">
            <h4>Thanh toán</h4>
            <p>Phuong th?c: @Model.PaymentMethod</p>
            <p>Tr?ng thái: @Model.Status</p>
        </div>
    </section>

    <section class="invoice-items">
        <table>
            <thead>
                <tr>
                    <th>S?n ph?m</th>
                    <th class="center">SL</th>
                    <th class="right">Giá</th>
                    <th class="right">Thành ti?n</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model.OrderItems)
                {
                    <tr>
                        <td>
                            @item.ProductName
                            @if (!string.IsNullOrEmpty(item.ProductName))
                            {
                                <br />
                                <small style="color: #666;">Phân lo?i: @item.ProductName</small>
                            }
                        </td>
                        <td class="center">@item.Quantity</td>
                        <td class="right">@item.UnitPrice.ToString("N0")d</td>
                        <td class="right">@item.Total.ToString("N0")d</td>
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="3">T?m tính</td>
                    <td class="right">@Model.TotalAmount.ToString("N0")d</td>
                </tr>
                @if(Model.DiscountAmount > 0)
                {
                    <tr>
                        <td colspan="3">Gi?m giá (@Model.VoucherCode ?? "")</td>
                        <td class="right">-@Model.DiscountAmount.ToString("N0")d</td>
                    </tr>
                }
                <tr class="total">
                    <td colspan="3">T?ng thanh toán</td>
                    <td class="right">@Model.FinalAmount.ToString("N0")d</td>
                </tr>
            </tfoot>
        </table>
    </section>

    <footer class="invoice-footer">
        <p>Xin c?m on quý khách! H?n g?p l?i.</p>
    </footer>

    <div class="print-actions no-print">
        <button class="primary-action" onclick="window.print()"><i class="bi bi-printer"></i> In hóa don</button>
        <a class="soft-action" href="@Url.Action("Detail", "Orders", new { area = "Admin", id = Model.OrderId })">Quay l?i</a>
        <a class="primary-action" style="margin-left:8px; background:#0d6efd; border-color:#0d6efd;" href="@( (Environment.GetEnvironmentVariable("API_BASE_URL") ?? "http://localhost:5188").TrimEnd('/') + "/api/orders/" + Model.OrderId + "/invoice.pdf")">T?i PDF</a>
        <img id="invoice-barcode" alt="barcode" style="margin-left:12px; height:64px; width:180px; object-fit:contain; border:1px solid var(--border-color); padding:6px;" />
    </div>
</div>

    <script>
    const apiBase = (('@(Environment.GetEnvironmentVariable("API_BASE_URL") ?? "http://localhost:5188")').trim()).replace(/\/$/, '');
    const pdfUrl = apiBase + '/api/orders/' + @Model.OrderId + '/invoice.pdf';
    // Auto print when opened in a dedicated tab
    if (window.location.search.includes('print')) {
        setTimeout(() => window.print(), 300);
    }
    if (window.location.search.includes('download') || window.location.search.includes('dl=1')) {
        window.location.href = pdfUrl;
    }
    // Render Code128 barcode on client for visual invoice using JsBarcode CDN
    (function loadBarcode(){
        const script = document.createElement('script');
        script.src = 'https://cdn.jsdelivr.net/npm/jsbarcode@3.11.5/dist/JsBarcode.all.min.js';
        script.onload = () => {
            try{
                const el = document.getElementById('invoice-barcode');
                if(el) JsBarcode(el, 'OP-' + ('0000' + @Model.OrderId).slice(-4), {format: 'CODE128', height:40, displayValue:true});
            }catch(e){console.warn('barcode render failed', e)}
        };
        document.head.appendChild(script);
    })();
    </script>
